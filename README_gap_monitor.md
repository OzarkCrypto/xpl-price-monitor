# Hyperliquid vs Binance 가격 갭 모니터

Hyperliquid와 Binance 간의 실시간 가격 차이를 모니터링하고 차익거래 기회를 탐지하는 대시보드입니다.

## 기능

- 🔄 **실시간 가격 모니터링**: Hyperliquid와 Binance의 가격을 실시간으로 수집
- 📊 **가격 갭 계산**: 두 거래소 간의 가격 차이를 달러와 퍼센트로 표시
- 💰 **차익거래 기회 탐지**: 어느 거래소에서 구매하는 것이 유리한지 표시
- 📈 **시각화**: 실시간 가격 차트와 갭 히스토리 차트 제공
- 📊 **통계 정보**: 평균 갭, 최대 갭 등 통계 정보 제공

## 설치

### 1. 의존성 설치

```bash
pip install -r requirements_gap_monitor.txt
```

### 2. 서버 실행

```bash
# 방법 1: 실행 스크립트 사용
./run_gap_monitor.sh

# 방법 2: 직접 실행
python3 hyperliquid_binance_gap_server.py
```

### 3. 대시보드 접속

#### 로컬 접속
브라우저에서 다음 주소로 접속:
```
http://localhost:5001
```

#### 공개 접속 (다른 사람들도 접속 가능)

**방법 1: 같은 네트워크 접속 (가장 간단)**
```bash
./quick_public_access.sh
```
같은 WiFi에 있는 사람들은 표시된 IP 주소로 접속 가능합니다.

**방법 2: 인터넷 전체 공개 (ngrok 사용)**
```bash
# 1. ngrok 설치 (자동)
./install_ngrok.sh

# 2. ngrok 인증 토큰 설정
# https://ngrok.com 에서 계정 생성 후 토큰 복사
./ngrok config add-authtoken YOUR_TOKEN

# 3. 공개 접속 시작
./start_gap_monitor_public.sh
```
공개 URL이 표시되면 인터넷 어디서나 접속 가능합니다!

**방법 3: 클라우드 배포 (영구적)**
자세한 내용은 `DEPLOYMENT_PUBLIC.md` 참고

## 사용법

1. **심볼 입력**: 대시보드 상단의 입력란에 모니터링할 심볼을 입력합니다 (예: MONUSDT, BTCUSDT)
2. **모니터링 시작**: "모니터링 시작" 버튼을 클릭합니다
3. **데이터 확인**: 
   - 가격 비교 카드에서 두 거래소의 현재 가격 확인
   - 갭 표시에서 가격 차이 확인
   - 차익거래 기회 배지에서 구매 추천 거래소 확인
   - 차트에서 가격 추이와 갭 히스토리 확인

## API 엔드포인트

### 가격 갭 데이터 조회
```
GET /api/gap/<symbol>
```

응답 예시:
```json
{
  "gap": {
    "symbol": "MONUSDT",
    "hyperliquid_price": 1.2345,
    "binance_price": 1.2350,
    "gap": 0.0005,
    "gap_percentage": 0.04,
    "timestamp": 1234567890.123,
    "opportunity": "buy_on_hyperliquid"
  },
  "statistics": {
    "current_gap": 0.0005,
    "current_gap_percentage": 0.04,
    "avg_gap": 0.0003,
    "max_gap": 0.0010,
    "min_gap": -0.0002,
    "avg_gap_percentage": 0.02,
    "total_samples": 50
  }
}
```

### 갭 히스토리 조회
```
GET /api/gap/<symbol>/history
```

### 통계 정보 조회
```
GET /api/statistics/<symbol>
```

### 모니터링 시작
```
GET /api/start/<symbol>
```

## 주요 파일

- `hyperliquid_binance_gap_server.py`: Flask 서버 및 가격 데이터 수집 로직
- `templates/hyperliquid_binance_gap_dashboard.html`: 웹 대시보드
- `requirements_gap_monitor.txt`: Python 의존성 목록
- `run_gap_monitor.sh`: 실행 스크립트

## 기술 스택

- **Backend**: Python 3, Flask, aiohttp
- **Frontend**: HTML, CSS, JavaScript, Chart.js
- **APIs**: 
  - Hyperliquid API: `https://api.hyperliquid.xyz`
  - Binance Futures API: `https://fapi.binance.com`

## 주의사항

1. **API 제한**: 각 거래소의 API 사용 제한을 확인하고 준수하세요
2. **네트워크 연결**: 안정적인 인터넷 연결이 필요합니다
3. **실시간성**: 가격 데이터는 약 5초마다 업데이트됩니다
4. **차익거래 위험**: 실제 거래 전에 수수료, 슬리피지 등을 고려하세요

## 문제 해결

### 가격 데이터를 가져올 수 없는 경우

1. 네트워크 연결 확인
2. API 엔드포인트가 변경되었는지 확인
3. 심볼 이름이 올바른지 확인 (예: MONUSDT, BTCUSDT)
4. 서버 로그 확인

### 차트가 표시되지 않는 경우

1. 브라우저 콘솔에서 JavaScript 오류 확인
2. Chart.js 라이브러리가 로드되었는지 확인
3. 브라우저 캐시 삭제 후 재시도

## 라이선스

이 프로젝트는 개인 사용 목적으로 제작되었습니다.

